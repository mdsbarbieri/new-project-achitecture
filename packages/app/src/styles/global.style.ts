import { colors } from './colors';

const sizes = { xxs: 2, xm: 4, sm: 8, nm: 16, lg: 24, xlg: 32, zxlg: 40 };

const globalStyles = {
  spacing: sizes,
  border: {
    radius: sizes,
  },
  colors,
};

export default globalStyles;
